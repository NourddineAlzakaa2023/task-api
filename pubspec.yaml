name: task_api
description: Simple Task API using Dart + Shelf + SQLite
version: 1.0.0

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  sqlite3: ^2.4.0

